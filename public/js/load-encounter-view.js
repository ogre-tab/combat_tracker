class LoadEncounterView
{
    constructor(containerElement)
    {
        this.containerElement = containerElement;
        // show the form that lets the client pick an encounter to load
        // need to fetch data from the server to fill this list
    }
}
